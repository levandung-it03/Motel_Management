package com.motel_management.Main;

import com.motel_management.DataAccessObject.DAOInterface;
import com.motel_management.DataAccessObject.ElectricRangeDAO;
import com.motel_management.DataAccessObject.WaterRangeDAO;
import com.motel_management.Models.WaterRangeModel;
import com.motel_management.Views.*;

import java.util.ArrayList;

public class Main {
    public static void main(String[] args) {
        // Starting our application.
//        Application.createApplication();
//        WaterRangeDAO.getInstance().insert(new String[] {"W01", "Muc 1", "50", "1000"});
//        WaterRangeDAO.getInstance().insert(new WaterRangeModel("W03", "Muc 2", 10, 100));
//        ArrayList<WaterRangeModel> e = WaterRangeDAO.getInstance().selectAll();
//        e.forEach(ob -> {System.out.println(ob.getRangeId() + ' ' + ob.getRangeName() + ' ' + ob.getMaxRangeValue() + ' ' + ob.getPrice());});
//        ArrayList<WaterRangeModel> e = WaterRangeDAO.getInstance().selectByCondition("WHERE price > 1300");
//        e.forEach(ob -> {System.out.println(ob.getRangeId() + ' ' + ob.getRangeName() + ' ' + ob.getMaxRangeValue() + ' ' + ob.getPrice());});
    }
}